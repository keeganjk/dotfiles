#!/bin/sh

SRC_DIR="~/.local/src"
mkdir -p ~/.local/bin ~/.local/src ~/.local/share ~/.config $SRC_DIR

source ./programs

aur_handler() {
    cd /tmp
    git clone "$i" && cd "$i"
    
}

git_handler() {

}

sudo pacman -Syu
sudo pacman -S --needed "$DISTRO_REPO"

for i in $AUR; do
    aur_handler "$i"
done
